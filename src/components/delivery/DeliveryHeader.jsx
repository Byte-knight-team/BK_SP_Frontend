import AppHeader from "../common/AppHeader";

export default function DeliveryHeader() {
  return (
    <AppHeader
      title="Delivery Panel"
      subtitle="Delivery assignments and route updates"
    />
  );
}